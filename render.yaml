services:
  - type: web
    name: ai-text-converter
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    envVars:
      - key: EURI_API_KEY1
        sync: false
      - key: EURI_API_KEY2
        sync: false
